[package]
name = "example-basic"
version = "0.1.0"
edition = "2024"

[dependencies]
thrust-macros = { path = "../../thrust-macros" }

[build-dependencies]
thrust-build = { path = "../../thrust-build" }
