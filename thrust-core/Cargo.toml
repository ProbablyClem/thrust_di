[package]
name = "thrust-core"
version = "0.1.0"
edition = "2024"

[dependencies]
thrust-macros = { path = "../thrust-macros" }

[build-dependencies]
syn = { version = "2", features = ["full"] }
quote = "1"
proc-macro2 = "1"
walkdir = "2"
